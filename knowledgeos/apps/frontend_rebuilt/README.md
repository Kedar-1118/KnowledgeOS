# Frontend Rebuilt

This folder contains a scaffolded Vite + React + TypeScript + Tailwind frontend intended to replace the broken frontend. To run locally:

```bash
cd apps/frontend_rebuilt
npm install
npm run dev
```
